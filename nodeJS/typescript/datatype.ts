let message:String="typescript";
let salary:number=5000;
let age:number=23;
let ismsg:boolean=true;

let displayString=`message is ${message} salary is ${salary} age is ${age} ismsg is ${ismsg}`;
console.log(displayString);